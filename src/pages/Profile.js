import {ChangeProfile} from "../component/ChangeProfile";
import {useContext} from "react";
import { AppContext} from "../App";
export const Profile = () => {

    return (
        <h1>This is the Profile page and user is:
            {/*<ChangeProfile />*/}
        </h1>
    )
}